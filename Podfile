# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'manual_client' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for manual_client
  pod 'SwiftSocket'

  target 'manual_clientTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'manual_clientUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
